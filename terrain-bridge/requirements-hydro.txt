# Dependencies for the offline hydrology tooling (hydrology/ + scripts/).
#
# Deliberately NOT in requirements.txt: the running bridge service never imports
# `hydrology`, and keeping these out means deploying the tile server does not drag
# in numba/LLVM. Install only when you need to generate or visualise water maps.
-r requirements.txt
numba>=0.59          # priority-flood fill; heap-driven, does not vectorise
scipy>=1.11          # connected components for lake filtering
rasterio>=1.3        # GeoTIFF in/out with georeference passthrough
matplotlib>=3.8      # scripts/test_water_overlay.py only
